# Pinned to the versions the committed results were produced with (Python 3.11).
# Library upgrades can shift results slightly (e.g. scikit-learn 1.6 -> 1.9
# moved the Random Forest numbers), so pinning keeps them reproducible.
numpy==2.4.6
pandas==3.0.6
matplotlib==3.11.2
seaborn==0.13.2
scikit-learn==1.9.1
xgboost==3.2.0
graphviz==0.21
openpyxl==3.1.5
jupyter==1.1.1
nbconvert==7.17.1
pytest==9.1.1
nistchempy==2.0.0
beautifulsoup4==4.15.0
torch==2.14.0
botorch==0.18.1
rdkit==2026.3.6

# This project's own package (src/boiling_point), installed in editable mode
-e .
